import React from "react";

export const metadata = {
  title: "Explore Our Menu | Bookatable",
  description: "Restaurant's complete menu of food items",
};

export default function layout({ children }) {
  return <div>{children}</div>;
}
