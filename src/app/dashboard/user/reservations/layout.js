import React from "react";

export const metadata = {
  title: "Your Orders | Bookatable",
  description: "User's orders",
};

export default function layout({ children }) {
  return <div>{children}</div>;
}
